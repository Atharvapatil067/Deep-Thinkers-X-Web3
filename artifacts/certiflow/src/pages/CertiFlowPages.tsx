import { FormEvent, useMemo, useState } from 'react';
import { isAddress } from 'ethers';
import { Link } from 'react-router-dom';
import toast from 'react-hot-toast';
import {
  ArrowRight,
  BadgeCheck,
  BarChart3,
  Clock3,
  FileCheck2,
  Fingerprint,
  Globe2,
  GraduationCap,
  KeyRound,
  LockKeyhole,
  Plus,
  Search,
  Send,
  Shield,
  ShieldCheck,
  Sparkles,
  Trash2,
  Users,
  WalletCards,
  X,
} from 'lucide-react';
import {
  CertificateCard,
  CertificateDetails,
  EmptyState,
  Footer,
  QRCodeModal,
  StatCard,
  StatusBadge,
  VerifySearch,
  WalletButton,
} from '@/components/CertiFlowComponents';
import type { AuthorizedIssuer, Certificate } from '@/data/mockData';
import { demoWallet, fullDemoWallet, liveStats } from '@/data/mockData';
import type { WalletButtonProps } from '@/components/CertiFlowComponents';

export function LandingPage({ walletConnected, onConnect, onDisconnect }: { walletConnected: boolean; onConnect: () => void; onDisconnect: () => void }) {
  return <div className="cf-page-glow min-h-screen bg-[#0b0f22]"><section className="cf-grid absolute inset-x-0 top-0 h-[680px]" />
    <div className="relative"><div className="mx-auto max-w-7xl px-5 pb-20 lg:px-8 lg:pb-28">
      <div className="grid min-h-[650px] items-center gap-14 pb-10 pt-20 lg:grid-cols-[1.02fr_.98fr] lg:gap-16 lg:pt-24">
        <div className="cf-rise"><div className="mb-7 inline-flex items-center gap-2 rounded-full border border-cyan-300/20 bg-cyan-300/[.07] px-3 py-1.5 text-[11px] font-bold uppercase tracking-[.18em] text-cyan-200"><span className="size-1.5 rounded-full bg-cyan-200 shadow-[0_0_12px_#68f4ed]" /> A credential network for the real world</div><h1 className="cf-display max-w-3xl text-5xl font-bold leading-[.98] text-white sm:text-6xl lg:text-[76px]">Verify Credentials.<br /><span className="cf-gradient">Instantly. On-chain.</span></h1><p className="mt-7 max-w-xl text-base leading-7 text-slate-400 sm:text-lg">A trusted, portable home for academic achievement. Issue credentials once, let anyone verify them forever — without calling a university office.</p><div className="mt-9 flex flex-wrap gap-3"><Link to="/verify" data-testid="link-hero-verify" className="cf-focus cf-button inline-flex items-center gap-2 rounded-xl bg-cyan-300 px-5 py-3.5 text-sm font-extrabold text-[#09101f] shadow-[0_10px_36px_rgba(104,244,237,.14)]">Verify Certificate <ArrowRight size={16} /></Link><Link to="/issuer" data-testid="link-hero-issue" className="cf-focus cf-button inline-flex items-center gap-2 rounded-xl border border-white/15 bg-white/[.04] px-5 py-3.5 text-sm font-bold text-slate-100 hover:bg-white/[.08]">Issue Certificate <Plus size={16} className="text-cyan-200" /></Link></div><div className="mt-9 flex items-center gap-4 text-xs text-slate-500"><div className="flex -space-x-2">{['AC','NI','PA','CL'].map((item) => <span key={item} className="grid size-7 place-items-center rounded-full border-2 border-[#0b0f22] bg-gradient-to-br from-indigo-400 to-cyan-300 text-[9px] font-bold text-[#0b0f22]">{item}</span>)}</div><span><b className="text-slate-300">46 institutions</b> already building trust on CertiFlow</span></div></div>
        <div className="cf-rise cf-delay-2 relative"><div className="absolute -inset-8 rounded-[40px] bg-indigo-500/[.08] blur-3xl" /><div className="cf-glass cf-float relative rounded-[28px] p-4 sm:p-6"><div className="flex items-center justify-between px-1 pb-4"><div className="flex items-center gap-2"><span className="grid size-8 place-items-center rounded-lg bg-cyan-300/10"><Fingerprint size={16} className="text-cyan-200" /></span><span className="text-xs font-bold tracking-wide text-slate-300">CERTIFLOW / CREDENTIAL</span></div><span className="cf-mono text-[10px] text-slate-500">#001847</span></div><div className="relative overflow-hidden rounded-2xl border border-cyan-200/20 bg-gradient-to-br from-[#192450] via-[#141b43] to-[#10152f] p-6 sm:p-8"><div className="absolute right-[-30px] top-[-30px] size-40 rounded-full border border-cyan-200/15" /><div className="absolute right-2 top-2 size-24 rounded-full border border-cyan-200/10" /><p className="relative text-[10px] font-bold uppercase tracking-[.22em] text-cyan-200/75">Soulbound certificate of achievement</p><div className="relative mt-14"><p className="text-sm text-slate-400">This certifies that</p><h2 className="cf-display mt-2 text-3xl font-bold text-white sm:text-4xl">Maya Chen</h2><p className="mt-3 max-w-sm text-sm leading-6 text-slate-400">has successfully completed the requirements for a</p><p className="mt-2 text-xl font-bold text-slate-100">BSc Computer Science</p></div><div className="relative mt-14 flex items-end justify-between"><div><p className="text-[10px] uppercase tracking-widest text-slate-500">Issued by</p><p className="mt-1 text-sm font-semibold text-slate-200">Aurora University</p></div><div className="grid size-11 place-items-center rounded-xl border border-cyan-200/25 bg-cyan-300/10"><BadgeCheck size={24} className="text-cyan-200" /></div></div></div><div className="flex items-center justify-between px-1 pt-4"><span className="cf-mono text-[10px] text-slate-500">0x4b7c...98fd</span><span className="inline-flex items-center gap-1.5 text-[10px] font-bold uppercase tracking-[.14em] text-emerald-300"><span className="size-1.5 rounded-full bg-emerald-300" /> Verified on-chain</span></div></div><div className="cf-glass absolute -bottom-7 -left-7 hidden w-48 rounded-2xl p-4 sm:block"><div className="flex items-center gap-2 text-xs text-slate-400"><ShieldCheck size={15} className="text-emerald-300" /> Proof verified</div><p className="cf-mono mt-3 text-xs text-cyan-100">2.1 sec lookup</p></div></div>
      </div>
      <div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{liveStats.map((stat, i) => <StatCard key={stat.label} value={stat.value} label={stat.label} icon={[FileCheck2, ShieldCheck, GraduationCap, Clock3][i]} />)}</div>
    </div></div>
    <section className="border-y border-white/[.07] bg-[#0d1229]/55"><div className="mx-auto grid max-w-7xl gap-12 px-5 py-20 lg:grid-cols-[.7fr_1.3fr] lg:px-8 lg:py-28"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-cyan-200">How it works</p><h2 className="cf-display mt-4 max-w-md text-4xl font-bold leading-tight text-white">A better proof layer for every achievement.</h2><p className="mt-5 max-w-md text-sm leading-7 text-slate-400">CertiFlow turns a PDF and a registrar’s inbox into a credential that is cryptographically yours, instantly searchable, and impossible to quietly edit.</p></div><div className="grid gap-4 sm:grid-cols-3">{[{ no: '01', title: 'Issue', body: 'An authorized institution signs a credential and mints it to a student wallet.' }, { no: '02', title: 'Own', body: 'The student keeps a permanent, soulbound proof of what they accomplished.' }, { no: '03', title: 'Verify', body: 'Recruiters and partners confirm the record in seconds — no account required.' }].map((step) => <div key={step.no} className="cf-glass rounded-2xl p-5"><p className="cf-mono text-xs text-cyan-200">{step.no}</p><h3 className="cf-display mt-9 text-xl font-bold text-white">{step.title}</h3><p className="mt-3 text-sm leading-6 text-slate-500">{step.body}</p></div>)}</div></div></section>
    <section className="mx-auto max-w-7xl px-5 py-20 lg:px-8 lg:py-28"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-cyan-200">Built for trust</p><h2 className="cf-display mt-4 text-4xl font-bold text-white">The infrastructure credentials deserve.</h2></div><Link to="/verify" data-testid="link-explore-verify" className="inline-flex items-center gap-2 text-sm font-bold text-cyan-200 hover:text-white">Explore verification <ArrowRight size={15} /></Link></div><div className="mt-12 grid gap-4 md:grid-cols-2 lg:grid-cols-4">{[{ icon: Shield, title: 'Tamper evident', body: 'Every record is anchored to the Sepolia chain with a public transaction trail.' }, { icon: Globe2, title: 'Works anywhere', body: 'A single verification link works across hiring, admissions, and lifelong learning.' }, { icon: LockKeyhole, title: 'Student-owned', body: 'Credentials are non-transferable by design. Your achievement stays yours.' }, { icon: BarChart3, title: 'Issuer-grade', body: 'Bulk-ready controls, clear status, and a ledger you can confidently share.' }].map((feature) => <div key={feature.title} className="rounded-2xl border border-white/[.08] bg-white/[.025] p-6 transition-colors hover:border-cyan-200/20 hover:bg-white/[.045]"><feature.icon size={21} className="text-cyan-200" /><h3 className="cf-display mt-10 text-lg font-bold text-white">{feature.title}</h3><p className="mt-3 text-sm leading-6 text-slate-500">{feature.body}</p></div>)}</div></section>
    <Footer />
  </div>;
}

export function VerifyPage({ certificates }: { certificates: Certificate[] }) {
  const [query, setQuery] = useState('');
  const [searched, setSearched] = useState(false);
  const [loading, setLoading] = useState(false);
  const [selected, setSelected] = useState<Certificate | null>(null);
  const [qr, setQr] = useState<Certificate | null>(null);
  const results = useMemo(() => { const needle = query.trim().toLowerCase(); if (!needle) return []; return certificates.filter((cert) => [cert.tokenId, cert.wallet, cert.recipient, cert.issuer].some((value) => value.toLowerCase().includes(needle))); }, [certificates, query]);
  const submit = () => { if (!query.trim()) { toast.error('Enter a token ID or wallet address to search.'); return; } setLoading(true); setSearched(false); window.setTimeout(() => { setLoading(false); setSearched(true); }, 750); };
  return <div className="cf-page-glow min-h-screen bg-[#0b0f22]"><main className="relative mx-auto max-w-7xl px-5 pb-24 pt-14 lg:px-8 lg:pt-20"><div className="mx-auto max-w-3xl text-center cf-rise"><div className="mx-auto mb-6 grid size-14 place-items-center rounded-2xl border border-cyan-200/20 bg-cyan-300/10 text-cyan-200"><Search size={24} /></div><p className="text-xs font-bold uppercase tracking-[.2em] text-cyan-200">Public verification portal</p><h1 className="cf-display mt-4 text-5xl font-bold leading-[1.02] text-white sm:text-6xl">Proof, not promises.</h1><p className="mx-auto mt-5 max-w-xl text-base leading-7 text-slate-400">Search the public credential ledger by token ID or wallet address. No account. No MetaMask. No guesswork.</p></div><div className="mx-auto mt-10 max-w-3xl cf-rise cf-delay-1"><VerifySearch value={query} onChange={setQuery} onSubmit={submit} loading={loading} /><div className="mt-3 flex items-center justify-between px-2 text-xs text-slate-500"><span>Try <button type="button" data-testid="button-example-token" onClick={() => setQuery('CF-2024-001847')} className="font-semibold text-cyan-200 hover:underline">CF-2024-001847</button></span><span className="inline-flex items-center gap-1.5"><ShieldCheck size={13} className="text-emerald-300" /> Public read-only lookup</span></div></div>{searched && <div className="mx-auto mt-14 max-w-5xl cf-rise"><div className="mb-5 flex items-end justify-between"><div><p className="text-xs uppercase tracking-[.17em] text-slate-500">Ledger response</p><h2 data-testid="text-search-results" className="cf-display mt-2 text-2xl font-bold text-white">{results.length} {results.length === 1 ? 'credential' : 'credentials'} found</h2></div><span className="cf-mono text-xs text-slate-500">QUERY / {query}</span></div>{results.length ? <div className="grid gap-4 md:grid-cols-2">{results.map((cert) => <CertificateCard key={cert.id} certificate={cert} onView={() => setSelected(cert)} onQr={() => setQr(cert)} />)}</div> : <EmptyState title="No matching credential" description="Check the token ID or wallet address and try again. Remember, the lookup is case-insensitive." icon={Search} />}</div>}<div className="mx-auto mt-20 max-w-5xl border-t border-white/[.07] pt-10"><div className="grid gap-4 sm:grid-cols-3"><div className="rounded-2xl bg-white/[.025] p-5"><Shield size={18} className="text-cyan-200" /><p className="mt-4 text-sm font-bold text-slate-200">Public by design</p><p className="mt-2 text-xs leading-5 text-slate-500">Anyone can verify. Only authorized issuers can create records.</p></div><div className="rounded-2xl bg-white/[.025] p-5"><KeyRound size={18} className="text-indigo-200" /><p className="mt-4 text-sm font-bold text-slate-200">No wallet required</p><p className="mt-2 text-xs leading-5 text-slate-500">Verification is a public reading experience — MetaMask is never needed.</p></div><div className="rounded-2xl bg-white/[.025] p-5"><Fingerprint size={18} className="text-violet-200" /><p className="mt-4 text-sm font-bold text-slate-200">Chain anchored</p><p className="mt-2 text-xs leading-5 text-slate-500">Each result includes the transaction fingerprint behind the proof.</p></div></div></div></main><CertificateDetails certificate={selected} onClose={() => setSelected(null)} onQr={() => { setQr(selected); setSelected(null); }} /><QRCodeModal certificate={qr} onClose={() => setQr(null)} /></div>;
}

export function IssuerPage({ walletConnected, walletAddress, isSepolia, isAuthorizedIssuer, walletChainId, isMetaMaskAvailable, isConnecting, onConnect, onDisconnect, certificates, onMint }: { walletConnected: boolean; walletAddress: string | null; isSepolia: boolean; isAuthorizedIssuer: boolean; walletChainId: number | null; isMetaMaskAvailable: boolean; isConnecting: boolean; onConnect: WalletButtonProps['onConnect']; onDisconnect: () => void; certificates: Certificate[]; onMint: (certificate: Certificate) => Promise<void> }) {
  const [form, setForm] = useState({ name: '', wallet: '', title: '', field: '', grade: '' });
  const [submitting, setSubmitting] = useState(false);
  const [done, setDone] = useState<Certificate | null>(null);
  const update = (field: keyof typeof form, value: string) => setForm((current) => ({ ...current, [field]: value }));
  const submit = async (event: FormEvent) => { event.preventDefault(); if (!walletConnected) { toast.error('Connect MetaMask first, then authorize that wallet in Admin.'); return; } if (!isAuthorizedIssuer) { toast.error('This wallet is not authorized to issue. Open Admin and authorize it first.'); return; } if (!isSepolia) { toast.error('Switch MetaMask to Sepolia Testnet before minting.'); return; } if (Object.values(form).some((value) => !value.trim())) { toast.error('Complete every certificate field before minting.'); return; } setSubmitting(true); const minted: Certificate = { id: `cert-${Date.now()}`, tokenId: `CF-2024-${String(2000 + certificates.length).padStart(6, '0')}`, title: form.title, recipient: form.name, wallet: form.wallet, issuer: 'Aurora University', issuerWallet: walletAddress ?? demoWallet, issued: 'Just now', issuedISO: new Date().toISOString(), status: 'Verified', grade: form.grade, field: form.field, txHash: '0x9d21...mint', accent: 'cyan' }; await new Promise((resolve) => window.setTimeout(resolve, 1050)); await onMint(minted); setDone(minted); setForm({ name: '', wallet: '', title: '', field: '', grade: '' }); setSubmitting(false); toast.success('Credential minted to the student wallet.'); };
  const fields = [{ key: 'name', label: 'Student name', placeholder: 'e.g. Maya Chen' }, { key: 'wallet', label: 'Student wallet address', placeholder: '0x71C4A9...9A2E' }, { key: 'title', label: 'Credential title', placeholder: 'e.g. BSc Computer Science' }, { key: 'field', label: 'Field of study', placeholder: 'e.g. Computer Science' }, { key: 'grade', label: 'Result or classification', placeholder: 'e.g. First Class Honours' }] as const;
  return <div className="cf-page-glow min-h-screen bg-[#0b0f22]"><main className="relative mx-auto max-w-7xl px-5 pb-24 pt-12 lg:px-8 lg:pt-16"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-cyan-200">Issuer console</p><h1 className="cf-display mt-3 text-4xl font-bold text-white sm:text-5xl">Put achievement on record.</h1><p className="mt-3 max-w-xl text-sm leading-6 text-slate-400">Mint a permanent, student-owned certificate to the Sepolia credential network.</p></div><WalletButton connected={walletConnected} address={walletAddress} chainId={walletChainId} isMetaMaskAvailable={isMetaMaskAvailable} isConnecting={isConnecting} onConnect={onConnect} onDisconnect={onDisconnect} /></div><div className="mt-10 grid gap-5 lg:grid-cols-[1.1fr_.9fr]"><form onSubmit={submit} className="cf-glass rounded-3xl p-5 sm:p-7"><div className="flex items-center justify-between border-b border-white/[.08] pb-5"><div><h2 className="text-lg font-bold text-white">Mint a certificate</h2><p className="mt-1 text-xs text-slate-500">All fields become part of the public credential record.</p></div><span className="rounded-lg bg-cyan-300/10 px-2.5 py-1 text-[10px] font-bold uppercase tracking-widest text-cyan-200">Step 1 / 1</span></div><div className="mt-6 grid gap-5 sm:grid-cols-2">{fields.map((field) => <label key={field.key} className={field.key === 'title' ? 'sm:col-span-2' : ''}><span className="text-xs font-semibold text-slate-300">{field.label}</span><input data-testid={`input-mint-${field.key}`} value={form[field.key]} onChange={(event) => update(field.key, event.target.value)} placeholder={field.placeholder} className="cf-focus mt-2 w-full rounded-xl border border-white/[.1] bg-black/15 px-3.5 py-3 text-sm text-white outline-none placeholder:text-slate-600 focus:border-cyan-200/50" /></label>)}</div><div className="mt-7 flex flex-col gap-3 rounded-xl border border-indigo-300/15 bg-indigo-300/[.06] p-4 text-xs leading-5 text-slate-400 sm:flex-row sm:items-start"><ShieldCheck size={16} className="shrink-0 text-indigo-200" /><span>Minting creates a non-transferable credential. The student wallet can receive and present it, but cannot sell or transfer it.</span></div><button type="submit" data-testid="button-mint-certificate" disabled={submitting} className="cf-button mt-6 inline-flex w-full items-center justify-center gap-2 rounded-xl bg-cyan-300 px-5 py-3.5 text-sm font-extrabold text-[#09101f] disabled:cursor-wait disabled:opacity-70">{submitting ? <><span className="size-4 animate-spin rounded-full border-2 border-[#09101f]/30 border-t-[#09101f]" /> Anchoring on Sepolia...</> : <><Send size={16} /> Mint certificate</>}</button></form><div className="space-y-5"><div className="cf-glass rounded-3xl p-6"><div className="flex items-center gap-3"><div className="grid size-10 place-items-center rounded-xl bg-emerald-300/10 text-emerald-300"><WalletCards size={19} /></div><div><p className="text-sm font-bold text-white">Issuer wallet</p><p data-testid="text-issuer-wallet" className="cf-mono mt-1 text-xs text-slate-500">{walletConnected && walletAddress ? walletAddress : 'Not connected'}</p></div></div>{walletConnected && isSepolia ? <p className="mt-5 flex items-center gap-2 text-xs text-emerald-300"><span className="size-1.5 rounded-full bg-emerald-300" /> Connected to Sepolia Testnet</p> : walletConnected ? <p className="mt-5 flex items-center gap-2 text-xs text-amber-200"><span className="size-1.5 rounded-full bg-amber-300" /> Switch MetaMask to Sepolia before minting.</p> : <p className="mt-5 text-xs leading-5 text-slate-500">Connect the authorized wallet using MetaMask to enable minting.</p>}</div>{done ? <div className="cf-glass rounded-3xl border-cyan-200/20 p-6"><div className="flex items-center justify-between"><p className="text-xs font-bold uppercase tracking-widest text-cyan-200">Mint successful</p><button type="button" data-testid="button-dismiss-success" onClick={() => setDone(null)} className="text-slate-500 hover:text-white"><X size={15} /></button></div><h3 className="cf-display mt-3 text-2xl font-bold text-white">{done.title}</h3><p className="mt-1 text-sm text-slate-400">{done.recipient}</p><div className="mt-5 rounded-xl border border-white/[.08] bg-black/10 p-3"><p className="cf-mono text-xs text-cyan-100">{done.tokenId}</p><p className="mt-1 text-[11px] text-slate-500">Transaction {done.txHash}</p></div><Link to="/verify" data-testid="link-view-minted" className="mt-4 inline-flex items-center gap-2 text-sm font-bold text-cyan-200">View public proof <ArrowRight size={14} /></Link></div> : <div className="rounded-3xl border border-dashed border-white/10 p-6"><Sparkles size={18} className="text-indigo-200" /><p className="mt-5 text-sm font-bold text-slate-300">Your next credential lands here.</p><p className="mt-2 text-xs leading-5 text-slate-500">Mint a certificate to see the signed record and transaction fingerprint.</p></div>}</div></div></main></div>;
}

export function CredentialsPage({ walletConnected, walletAddress, walletChainId, isMetaMaskAvailable, isConnecting, onConnect, onDisconnect, certificates }: { walletConnected: boolean; walletAddress: string | null; walletChainId: number | null; isMetaMaskAvailable: boolean; isConnecting: boolean; onConnect: WalletButtonProps['onConnect']; onDisconnect: () => void; certificates: Certificate[] }) {
  const [filter, setFilter] = useState<'All' | 'Verified'>('All');
  const [selected, setSelected] = useState<Certificate | null>(null);
  const [qr, setQr] = useState<Certificate | null>(null);
  const visible = certificates.filter((cert) => filter === 'All' || cert.status === filter).filter((cert) => Boolean(walletAddress) && cert.wallet.toLowerCase() === walletAddress?.toLowerCase());
  return <div className="cf-page-glow min-h-screen bg-[#0b0f22]"><main className="relative mx-auto max-w-7xl px-5 pb-24 pt-12 lg:px-8 lg:pt-16"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-cyan-200">Student vault</p><h1 className="cf-display mt-3 text-4xl font-bold text-white sm:text-5xl">Your proof, together.</h1><p className="mt-3 max-w-lg text-sm leading-6 text-slate-400">A private collection of public achievements. Share what matters, when it matters.</p></div><WalletButton connected={walletConnected} address={walletAddress} chainId={walletChainId} isMetaMaskAvailable={isMetaMaskAvailable} isConnecting={isConnecting} onConnect={onConnect} onDisconnect={onDisconnect} /></div>{!walletConnected ? <div className="cf-glass mx-auto mt-12 max-w-xl rounded-3xl p-8 text-center"><div className="mx-auto grid size-14 place-items-center rounded-2xl bg-indigo-300/10 text-indigo-200"><WalletCards size={25} /></div><h2 className="cf-display mt-5 text-2xl font-bold text-white">Connect to open your vault</h2><p className="mt-3 text-sm leading-6 text-slate-400">Your certificates are tied to your wallet — connect MetaMask to view the collection owned by the selected account.</p><button type="button" data-testid="button-connect-vault" onClick={onConnect} className="cf-button mt-6 rounded-xl bg-cyan-300 px-5 py-3 text-sm font-bold text-[#09101f]">Connect MetaMask</button></div> : <><div className="mt-10 grid gap-3 sm:grid-cols-3"><div className="cf-glass rounded-2xl p-5"><p className="text-xs text-slate-500">Owned credentials</p><p data-testid="text-credential-count" className="cf-display mt-2 text-3xl font-bold text-white">{visible.length}</p></div><div className="cf-glass rounded-2xl p-5"><p className="text-xs text-slate-500">Latest achievement</p><p className="cf-display mt-2 truncate text-lg font-bold text-white">{visible[0]?.field ?? '—'}</p></div><div className="cf-glass rounded-2xl p-5"><p className="text-xs text-slate-500">Vault status</p><p className="mt-2 inline-flex items-center gap-2 text-sm font-bold text-emerald-300"><span className="size-2 rounded-full bg-emerald-300" /> {walletChainId === 11155111 ? 'Sepolia connected' : 'Switch to Sepolia'}</p></div></div><div className="mt-12 flex items-center justify-between"><div><h2 className="text-lg font-bold text-white">Credential collection</h2><p className="mt-1 text-xs text-slate-500">Only you can receive these records. Anyone can verify them.</p></div><div className="flex rounded-xl border border-white/[.08] bg-white/[.03] p-1">{(['All', 'Verified'] as const).map((item) => <button type="button" key={item} data-testid={`button-filter-${item.toLowerCase()}`} onClick={() => setFilter(item)} className={`rounded-lg px-3 py-2 text-xs font-bold ${filter === item ? 'bg-cyan-300/15 text-cyan-200' : 'text-slate-500 hover:text-slate-300'}`}>{item}</button>)}</div></div><div className="mt-5 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{visible.length ? visible.map((cert) => <CertificateCard key={cert.id} certificate={cert} onView={() => setSelected(cert)} onQr={() => setQr(cert)} />) : <div className="md:col-span-2 lg:col-span-3"><EmptyState title="No credentials in this wallet" description="This account has no mock certificates yet. When contract reads are added, this collection will load from its on-chain records." icon={FileCheck2} /></div>}</div></>}</main><CertificateDetails certificate={selected} onClose={() => setSelected(null)} onQr={() => { setQr(selected); setSelected(null); }} /><QRCodeModal certificate={qr} onClose={() => setQr(null)} /></div>;
}

export function AdminPage({ issuers, walletAddress, onConnect, onAdd, onRevoke }: { issuers: AuthorizedIssuer[]; walletAddress: string | null; onConnect: WalletButtonProps['onConnect']; onAdd: (issuer: AuthorizedIssuer) => void; onRevoke: (id: string) => void }) {
  const [name, setName] = useState('');
  const [wallet, setWallet] = useState('');
  const submit = (event: FormEvent) => { event.preventDefault(); const normalizedWallet = wallet.trim(); if (!name.trim() || !normalizedWallet) { toast.error('Add an institution name and wallet address.'); return; } if (!isAddress(normalizedWallet)) { toast.error('Enter a valid Ethereum wallet address beginning with 0x.'); return; } if (issuers.some((issuer) => issuer.status === 'Active' && issuer.wallet.toLowerCase() === normalizedWallet.toLowerCase())) { toast.error('That wallet is already authorized.'); return; } onAdd({ id: `issuer-${Date.now()}`, name: name.trim(), wallet: normalizedWallet, added: 'Just now', status: 'Active' }); setName(''); setWallet(''); toast.success('Issuer wallet authorized.'); };
  return <div className="cf-page-glow min-h-screen bg-[#0b0f22]"><main className="relative mx-auto max-w-7xl px-5 pb-24 pt-12 lg:px-8 lg:pt-16"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-cyan-200">Protocol administration</p><h1 className="cf-display mt-3 text-4xl font-bold text-white sm:text-5xl">Control who can issue.</h1><p className="mt-3 max-w-xl text-sm leading-6 text-slate-400">Manage the institution wallets trusted to write to the CertiFlow credential ledger.</p></div><div className="mt-10 grid gap-5 lg:grid-cols-[.75fr_1.25fr]"><form onSubmit={submit} className="cf-glass h-fit rounded-3xl p-6"><div className="grid size-11 place-items-center rounded-2xl bg-indigo-300/10 text-indigo-200"><KeyRound size={21} /></div><h2 className="mt-5 text-lg font-bold text-white">Authorize issuer</h2><p className="mt-2 text-xs leading-5 text-slate-500">Add a verified institution wallet to grant minting permissions.</p><label className="mt-6 block"><span className="text-xs font-semibold text-slate-300">Institution name</span><input data-testid="input-issuer-name" value={name} onChange={(event) => setName(event.target.value)} placeholder="e.g. Meridian College" className="cf-focus mt-2 w-full rounded-xl border border-white/[.1] bg-black/15 px-3.5 py-3 text-sm text-white outline-none placeholder:text-slate-600 focus:border-cyan-200/50" /></label><label className="mt-4 block"><span className="flex items-center justify-between text-xs font-semibold text-slate-300"><span>Wallet address</span><button type="button" onClick={() => walletAddress ? setWallet(walletAddress) : void onConnect()} className="font-bold text-cyan-200 hover:text-cyan-100">{walletAddress ? 'Use connected wallet' : 'Connect wallet'}</button></span><input data-testid="input-issuer-wallet" value={wallet} onChange={(event) => setWallet(event.target.value)} placeholder="0x..." className="cf-focus mt-2 w-full rounded-xl border border-white/[.1] bg-black/15 px-3.5 py-3 text-sm text-white outline-none placeholder:text-slate-600 focus:border-cyan-200/50" /></label><button type="submit" data-testid="button-authorize-issuer" className="cf-button mt-6 inline-flex w-full items-center justify-center gap-2 rounded-xl bg-cyan-300 px-4 py-3 text-sm font-bold text-[#09101f]"><Plus size={16} /> Authorize wallet</button></form><section className="cf-glass rounded-3xl p-5 sm:p-7"><div className="flex items-center justify-between"><div><h2 className="text-lg font-bold text-white">Authorized issuers</h2><p className="mt-1 text-xs text-slate-500">{issuers.filter((issuer) => issuer.status === 'Active').length} active wallets can mint</p></div><span className="inline-flex items-center gap-2 rounded-full border border-emerald-300/20 bg-emerald-300/10 px-3 py-1.5 text-xs font-bold text-emerald-200"><span className="size-1.5 rounded-full bg-emerald-300" /> Network healthy</span></div><div className="mt-6 divide-y divide-white/[.07]">{issuers.map((issuer) => <div key={issuer.id} data-testid={`row-issuer-${issuer.id}`} className="flex flex-col gap-4 py-4 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-3"><div className="grid size-10 place-items-center rounded-xl bg-white/[.06] text-slate-300"><GraduationCap size={18} /></div><div><p className="text-sm font-bold text-slate-200">{issuer.name}</p><p className="cf-mono mt-1 text-[11px] text-slate-500">{issuer.wallet}</p></div></div><div className="flex items-center gap-3"><StatusBadge status={issuer.status} />{issuer.status === 'Active' && <button type="button" data-testid={`button-revoke-${issuer.id}`} onClick={() => { onRevoke(issuer.id); toast.success(`${issuer.name} access revoked.`); }} className="cf-focus rounded-lg p-2 text-slate-500 transition-colors hover:bg-rose-300/10 hover:text-rose-200"><Trash2 size={15} /></button>}</div></div>)}</div>{issuers.length === 0 && <EmptyState title="No issuers authorized" description="Add the first institution wallet to begin writing credentials." icon={Users} />}</section></div><div className="mt-5 flex items-center gap-3 rounded-2xl border border-amber-300/15 bg-amber-300/[.05] p-4 text-xs leading-5 text-amber-100/70"><Shield size={16} className="shrink-0 text-amber-200" /> This is a simulated admin console. Wallet authorization changes are stored in the current session only.</div></main></div>;
}
